<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="using:JeedomApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../ConvertersRessources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Template pour un equipement On Off Slider  -->
    <DataTemplate x:Key="OnOffSliderEqLogicTemplate">
        <Controls:TileControl
            Title="{Binding Name, FallbackValue=Name}"
            d:DesignHeight="240"
            d:DesignWidth="240"
            SubTitle="{Binding StateBool, ElementName=OnOff}">
            <Controls:OnOffSliderPluginControl x:Name="OnOff" DataContext="{Binding}">
                <Controls:OnOffSliderPluginControl.OnBrush>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Offset="0" Color="#FF54AD0C" />
                        <GradientStop Offset="1" Color="#FF81BC16" />
                    </LinearGradientBrush>
                </Controls:OnOffSliderPluginControl.OnBrush>
            </Controls:OnOffSliderPluginControl>
        </Controls:TileControl>
    </DataTemplate>
</ResourceDictionary>